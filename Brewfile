# Brewfile — run `brew bundle` to install all required tools
# or `make setup` which calls this automatically.

# direnv — loads .envrc automatically when entering the repo directory
brew "direnv"

# Azure Functions Core Tools
tap "azure/functions"
brew "azure-functions-core-tools@4"
